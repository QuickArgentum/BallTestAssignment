namespace Data
{
    /// <summary>
    /// This enum describes all the possible poolable prefab types. They can be instantiated by passing
    /// the corresponding enum value
    /// </summary>
    public enum PrefabType
    {
        None,
        Projectile,
        Obstacle,
        Explosion,
        ObstacleDeathEffect,
        ProjectileBreakEffect
    }
}